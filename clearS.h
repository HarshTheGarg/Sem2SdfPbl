// ./clearS.h

// (Preprocessor) If the CLEARS_H header is not defined, so that it is not defined multiple times
#ifndef CLEARS_H

// Define the CLEARS_H header
#define CLEARS_H

// signature of the function defined in the clearS.cpp file
void clear();

// Ending the preprocessor if statement
#endif